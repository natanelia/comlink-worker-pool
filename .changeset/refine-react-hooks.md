---
"comlink-worker-pool-react": minor
---

Expose initializing, ready, error, and closed pool lifecycle state, add awaitable manual close, introduce a fully inferred method-bound `useWorkerTask` hook, and use a conservative capped default pool size.
