---
"comlink-worker-pool": minor
"comlink-worker-pool-react": minor
---

Add structured task and worker lifecycle events plus queue age, capacity, lifecycle state, and cumulative outcome counters to pool statistics. Forward event observers through the React hook without recreating its pool.
